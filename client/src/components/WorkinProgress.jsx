import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';


const InProgressPage = () => (
  <Card className="container">
    <CardTitle title="Work is in Progress" subtitle="In Progress." />
  </Card>
);

export default InProgressPage;
